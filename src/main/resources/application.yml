server:
  port: 9999
  contextPath: /

spring:
  profiles.active: local
  jpa:
    hibernate:
      ddl-auto: none
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  datasource:
    url: jdbc:mysql://www.bookstorage.kr:3308/testtest?characterEncoding=utf8&useUnicode=true&autoReconnect=true&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: "password"
    driver-class-name: com.mysql.jdbc.Driver

data.path: data
